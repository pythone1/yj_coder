# 可辨识性分析报告

## 1. 先说结论

当前结果不如早期工程版本，主要不是因为算法链路坏了，而是因为问题现在更接近真实溯源定义，辨识难度更高。

这次分析重点回答了三个问题：

1. 10 个候选节点之间，哪些点的单井响应本来就非常像。
2. 当前注水实验在 4 个监测点上的信号强度是否足够明显。
3. 4 个监测点里，哪些点真正提供了区分能力。

## 2. 当前最容易混淆的点

- 相关性最高的节点对平均相关性约为：0.739
- 真实点最近邻混淆对中，最高相关性约为：0.804

从当前结果看，最关键的混淆主要集中在真实点及其邻近井：

- 真值点 `J129` 最容易和 `J130` 混淆，相关性 `0.513`，距离 `4.043`
- 真值点 `J129` 最容易和 `J127` 混淆，相关性 `0.504`，距离 `4.200`
- 真值点 `J129` 最容易和 `J128` 混淆，相关性 `0.489`，距离 `3.901`
- 真值点 `J195` 最容易和 `J72` 混淆，相关性 `0.804`，距离 `2.063`
- 真值点 `J195` 最容易和 `J194` 混淆，相关性 `0.768`，距离 `2.364`
- 真值点 `J195` 最容易和 `J128` 混淆，相关性 `0.383`，距离 `4.022`
- 真值点 `J61` 最容易和 `J60` 混淆，相关性 `0.631`，距离 `3.341`
- 真值点 `J61` 最容易和 `J127` 混淆，相关性 `0.382`，距离 `4.541`
- 真值点 `J61` 最容易和 `J128` 混淆，相关性 `0.294`，距离 `4.399`

## 3. 注水强度是否偏弱

- 当前真实工况（scale=1.0）下，4 个监测点平均 peak delta 为 `0.2055`
- 当前真实工况（scale=1.0）下，4 个监测点平均 SNR(std ratio) 为 `3.7194`
- 当注水放大到 scale=3.0 时，平均 peak delta 提升到 `0.2878`，平均 SNR 提升到 `5.9908`

这说明注水强度越大，监测端的信号区分度会同步提高。若当前工况下响应峰值和 SNR 偏低，确实会加剧邻近井代偿。

## 4. 监测点布设是否合理

下表反映了不同监测点对区分真值点与其他候选点的平均贡献。`mean_distance` 越大，说明该监测点越能把真值点和其他候选点拉开。

- 真值点 `J129` 在监测点 `J145` 上的平均区分距离为 `0.8314`，最大相关性为 `1.0000`
- 真值点 `J129` 在监测点 `J236` 上的平均区分距离为 `0.2266`，最大相关性为 `0.6268`
- 真值点 `J129` 在监测点 `J59` 上的平均区分距离为 `0.0340`，最大相关性为 `0.5091`
- 真值点 `J129` 在监测点 `J17` 上的平均区分距离为 `0.0017`，最大相关性为 `0.5021`
- 真值点 `J195` 在监测点 `J145` 上的平均区分距离为 `0.5601`，最大相关性为 `1.0000`
- 真值点 `J195` 在监测点 `J236` 上的平均区分距离为 `0.3244`，最大相关性为 `0.9172`
- 真值点 `J195` 在监测点 `J59` 上的平均区分距离为 `0.0777`，最大相关性为 `0.9999`
- 真值点 `J195` 在监测点 `J17` 上的平均区分距离为 `0.0016`，最大相关性为 `0.6935`
- 真值点 `J61` 在监测点 `J145` 上的平均区分距离为 `0.5994`，最大相关性为 `0.9421`
- 真值点 `J61` 在监测点 `J236` 上的平均区分距离为 `0.2290`，最大相关性为 `0.6578`
- 真值点 `J61` 在监测点 `J59` 上的平均区分距离为 `0.0342`，最大相关性为 `0.5875`
- 真值点 `J61` 在监测点 `J17` 上的平均区分距离为 `0.0017`，最大相关性为 `0.7578`

## 5. 综合判断

当前结果不够好，最可能是两个原因叠加：

1. **监测点布设辨识度不够**：部分真值点与邻近候选点在现有 4 个监测点上的响应太像。
2. **注水激励强度偏弱或差异不够明显**：监测点能感知到异常，但不一定能清楚分出各支路的独特指纹。

也就是说，问题已经不主要是算法主链错误，而是“实验设计 + 可辨识性”成为瓶颈。

## 6. 建议

1. 优先考虑增强注水激励，或者让 3 个注水点的时序形态更有差异。
2. 重新评估监测点布设，重点增强对 `J129` 这类偏弱支路的约束。
3. 后续算法优化应把重点放在拓扑约束、时延特征和 posterior 判别逻辑，而不是只继续堆拟合分数。
